# ==============================================================
# PROFIL DEV — chargé uniquement avec --spring.profiles.active=dev
# Identifiants du seed de données (DataInitializer).
# Surchargeables par variables d'environnement ; ne JAMAIS utiliser ces
# valeurs par défaut en production (le seed n'y est de toute façon pas actif).
# ==============================================================
app.seed.admin-email=${SEED_ADMIN_EMAIL:admin@cdgcapital.ma}
app.seed.admin-password=${SEED_ADMIN_PASSWORD:admin123}
app.seed.consultant-password=${SEED_CONSULTANT_PASSWORD:consultant123}
